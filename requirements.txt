# Web Framework & API Server
fastapi>=0.110
uvicorn[standard]>=0.29
httpx>=0.27
pyyaml>=6.0
python-dotenv>=1.0.0
psutil>=5.9.0

# Testing & Diagnostics
pytest>=8.0
pytest-asyncio>=0.23

# Local Laya Router Backend & Machine Learning
# - macOS (Apple Silicon M1/M2/M3/M4): Standard PyPI wheels provide native Metal (MPS) GPU acceleration out-of-the-box.
# - Windows / Linux: Standard install provides CPU inference (~10ms). For NVIDIA CUDA GPU acceleration,
#   install via: pip install torch --index-url https://download.pytorch.org/whl/cu124 (or run python install.py)
laya>=0.3.6
torch>=2.0.0

